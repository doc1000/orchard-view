[project]
name = "orchard-view"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "jupyter>=1.1.1",
    "openai>=3.1.0",
    "orchard[ml]",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]

[tool.uv.sources]
orchard = { path = "../orchard", editable = true }
